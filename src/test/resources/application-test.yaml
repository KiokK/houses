spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:tc:postgresql:15.1:///test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test

  liquibase:
    enabled: true
